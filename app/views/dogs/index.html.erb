<%= form_tag dogs_path, method: :get, local: true do |form| %>
  <%= select_tag :order, options_for_select(list_of_sorting_methods, params[:order]) %>
  <%= submit_tag "Sort" %>
<% end%>

<%= paginate @dogs %>
<%= render partial: 'thumbnail', collection: @dogs, as: :dog %>
<%= paginate @dogs %>
